import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Simone's Train",
  description: "A chatbot powered by the knowledge graph of overheard conversations on the Torino-Salerno train",
  keywords: ["train", "chatbot", "knowledge graph", "conversations", "AI"],
  openGraph: {
    title: "Simone's Train",
    description: "Ask questions about what's happening on this loud Italian train",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
